# tgo-govind

Govind Das VSL landing page. Next.js 14 (App Router). Built from the v4 copy
(`public/assets/source/copy/govind-vsl-landing-page-v4.md`) via the SHAPE agent.

```bash
npm install
npm run dev        # http://localhost:3000
```

- Skin + design decisions: `design-system.project.md`
- The one next click is **Book My Session** (paid, online, one-on-one). The Shark Sage
  program is reserved for the room and is correctly absent from this page.

## Open items (from the copy's CONFIRM list — wire before launch)
- Booking link + deposit price for the online session (CTA currently `href="#book"`)
- Real VSL film → drop at `/public/vsl.mp4`, set `VSL_SRC` in `app/FunnelScripts.tsx`, add a poster image
- Consented/anonymised coaching quotes + written sign-off from every named Lab association
- Defensible source/wording for the science claims before paid traffic
- Real per-week capacity so "limited slots" is literally true
# tgo-govind
